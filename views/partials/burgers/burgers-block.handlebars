<li>
	{{burger_name}}

	{{#if devour}}
	<button class="change-devoured" data-id="{{id}}" data-newdevour="{{1}}">
		Eaten!
	</button>
	{{else}}

	<button class="delete-burger" data-id="{{id}}" data-newdevour="{{1}}">
		Delete
	</button>
	{{/if}}



</li>